# name
name: hello_world

# trigger
on:
- push

# one or more jobs
jobs:
    greetings:
        runs-on: ubuntu-latest
        steps:
        - id: s00
        # runs bash/zsh/cli tool
          run: echo "Current time in Github is $(date)"
        - id: s01
          run: cat ../hello.txt

